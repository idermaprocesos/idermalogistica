<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="IdermaFichas.Pages.RegistroOrdenesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IdermaFichas.Pages">

    <Grid Padding="20,16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="8">
            <Button HorizontalAlignment="Left" Click="Volver_Click" Content="Volver" />
            <TextBlock FontSize="20" FontWeight="SemiBold" Text="Órdenes anteriores" />
            <TextBlock
                x:Name="Subtitulo"
                Opacity="0.72"
                Text="Las órdenes exportadas quedan guardadas aquí. Puede volver a descargarlas en PDF o Excel."
                TextWrapping="Wrap" />
        </StackPanel>

        <InfoBar
            x:Name="BarraAviso"
            Grid.Row="1"
            IsClosable="True"
            IsOpen="False" />

        <Grid Grid.Row="2">
            <TextBlock
                x:Name="TextoVacio"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Opacity="0.72"
                Text="Aún no hay órdenes. Al exportar una orden de compra queda en este registro."
                TextAlignment="Center"
                TextWrapping="Wrap" />
            <ListView
                x:Name="Lista"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:OrdenRegistradaItem">
                        <Grid Padding="4,10" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="2">
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind Titulo}" />
                                <TextBlock Opacity="0.85" Text="{x:Bind Resumen}" TextWrapping="Wrap" />
                                <TextBlock Opacity="0.65" Text="{x:Bind Extra}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="8">
                                <Button
                                    Click="DescargarPdf_Click"
                                    Content="PDF"
                                    Tag="{x:Bind IdTexto}" />
                                <Button
                                    Click="DescargarExcel_Click"
                                    Content="Excel"
                                    Tag="{x:Bind IdTexto}" />
                                <Button
                                    Click="Quitar_Click"
                                    Content="Quitar"
                                    Tag="{x:Bind IdTexto}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
